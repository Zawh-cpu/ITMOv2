## План переписывания `tests_load.md`

### Факты из кода (единственный источник истины — `TRAINING_PR.diff`)

Проверил diff построчно, вот что там реально есть и на что можно опираться:

1. **`app/api.py:36`** — `def create_review(payload: dict) -> dict[str, str]: return review_service.review(payload["diff"])`. Обработчик объявлен как обычный `def`, не `async def`.
2. **`app/review_service.py:19-22`** — `def review(self, diff: str) -> dict[str, str]: prompt = f"..."; answer = self.llm.generate(prompt); return {"comment": answer}`. Вызов `llm.generate` **синхронный и блокирующий**, без `try/except`.
3. **`app/review_service.py:9-12`** — `LLM.generate` в `Protocol` объявлен как `def`, не `async def`.
4. **`app/api.py:40-42`** — `def health()` тоже `def`, не зависит от LLM, просто возвращает `{"status": "ok"}`.
5. Нет валидации размера `payload["diff"]`, нет проверки наличия ключа `"diff"`, нет ограничения длины prompt.

Ключевой вывод для нагрузочного плана: **все три функции (`create_review`, `review`, `generate`) — sync `def`**, а в FastAPI такие обработчики уходят в общий threadpool. Это прямое основание для теста на конкуренцию за пул потоков (в старом файле это было только гипотезой — "поведение пула потоков не проверялось", теперь есть прямая цитата, подтверждающая, зачем этот тест нужен).

### Факты из `problem.md`

- Учебный diff маленький, реальный размер diff и реальная команда/лимиты неизвестны (строка 15).
- Вне задачи: аутентификация, лимиты самого сервиса, логи, интеграция с Git-хостингом (строка 11) — эти проверки в новый файл не включаю.
- Целевые значения метрик в problem.md сами помечены как мои допущения, а не как SLA — значит их нельзя использовать как "предел" для нагрузочных тестов без дополнительной пометки.

### Структура нового файла (строго по формату из задания)

1. **Предупреждение** — 2-3 строки: числа ниже либо из цитаты diff/problem.md, либо явное допущение с причиной, либо "нет источника — вопрос".
2. **Таблица** (≤5 сценариев), столбцы: `Сценарий | Что в коде это вызывает (цитата из diff) | Нагрузка | Предел | Источник предела | Что измеряем | Чем подтверждаем`.
3. **Что спросить у владельца сервиса** — список вопросов без чисел от меня.
4. **Когда нагрузочное тестирование нужно**.

### Черновой набор сценариев (4, не больше 5)

| # | Сценарий | Обоснование из кода | Предел — источник |
|---|---|---|---|
| 1 | Базовая нагрузка на `/api/reviews` (fake LLM без задержки) | цитата `create_review`→`review`→`llm.generate` | нет источника на латентность/SLA → вопрос владельцу, не число |
| 2 | Конкуренция за threadpool (fake LLM с задержкой) | цитата: все три уровня — `def`, не `async def` (прямое следствие кода, не гипотеза) | нет источника на размер пула/ожидаемую конкурентность → вопрос |
| 3 | Diff без ограничения размера | цитата: `payload["diff"]` передаётся в `review()` без проверки длины | нет предела в коде и нет источника размера реального diff (problem.md подтверждает: "размер diff неизвестен") → вопрос |
| 4 | Необработанное исключение в `review()` | цитата: отсутствие `try/except` вокруг `self.llm.generate` | доля сбоев провайдера — нет источника → вопрос; но факт "код вернёт 500 по умолчанию" и "`/health` не зависит от LLM и должен остаться 200" — это утверждения из кода, не допущения |

Для каждой ячейки "Нагрузка" (число клиентов, длительность), где нет источника ни в diff, ни в problem.md, буду либо явно писать "допущение: минимальное число для наблюдения эффекта, причина — Х", либо, если и такого обоснования нет, — не ставить число и переносить вопрос в раздел "Что спросить у владельца сервиса".

### Вопросы владельцу сервиса (черновой список, без чисел)

- Целевая SLA-задержка и допустимая доля ошибок `/api/reviews`?
- Ожидаемое число одновременных ревью в проде и конфигурация threadpool (сколько воркеров)?
- Реальный размер diff в PR и есть ли лимит токенов/размера у провайдера LLM?
- Ожидаемая частота сбоев/таймаутов реального провайдера LLM?

### Открытые вопросы к вам перед записью файла

1. Текущий `tests_load.md` и `problem.md` заканчиваются разделом **"Как использовали AI"** со ссылками на `prompts.md`. Новый формат, который вы задали, такого раздела не предусматривает. Оставить его для консистентности с остальными файлами проекта или убрать полностью, как в заданном формате?
2. Старый файл ссылался на `project_management.md` (инкремент 4) в разделе "когда нужно тестирование". Этот файл не входит в перечисленные вами источники для этой задачи — сохранить ссылку (она не выдумана, файл существовал в исходном плане) или убрать, оставив только условие в общих словах без ссылки на конкретный документ?

После вашего ответа зафиксирую финальный текст и (в режиме выполнения) запишу его в `tests_load.md`.