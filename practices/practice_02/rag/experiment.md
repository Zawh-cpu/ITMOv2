# RAG

Этот эксперимент выполнил Claude Sonnet 5 Thinking внутри сессии, без отдельного запуска и без другой внешней модели: я читал только файлы из таблицы ниже и больше ничего не открывал. Поэтому сырого вывода (`result.jsonl`) и `cmd.txt` нет. Запрос записан дословно, как я его принял, ответ ниже.

- Вопрос к источникам: что из пределов и допущений в `tests_load.md` можно подтвердить источником, а что нет.

## Разрешённые источники

Номера строк везде относятся к самому файлу, включая `TRAINING_PR.diff` (строки файла diff, не строки исходников `app/api.py` и `app/review_service.py`).

| Файл или документ | Зачем нужен | Какой фрагмент используем |
|---|---|---|
| [`practice_01/TRAINING_PR.diff`](../../practice_01/TRAINING_PR.diff) | единственный источник о коде: эндпоинты, вызов LLM | строки 19-22 (`review`), 35-37 (`create_review`), 40-42 (`health`), 10-12 (`LLM.generate`) |
| [`practice_01/problem.md`](../../practice_01/problem.md) | что известно о размере diff, команде, что вне задачи | строки 11 и 15 |
| [`practice_01/context.md`](../../practice_01/context.md) | что неизвестно про реализацию LLM, лимиты, команду | строки 13, 55, 56, 59 |
| [`practice_01/project_management.md`](../../practice_01/project_management.md) | на него ссылается `tests_load.md` (инкремент 4) | строки 20 и 21 |
| [`practice_01/prompts.md`](../../practice_01/prompts.md) | только чтобы проверить ссылку «строка 12 (P1-04)» из `tests_load.md` | строка 12 |

`practice_01/README.md` я только просмотрел `grep` по словам про нагрузку и пределы: там строка 67 («нагрузочные проверки и ожидаемые пределы») и строка 225 с весом критерия. Чисел, SLA и пределов не нашёл, поэтому в таблицу его не включил. В `OPENCODE_SETUP.md` искал слова про пул потоков и пределы, совпадений нет, тоже не включил.

## Запрос

Запрос к «модели» (мне), дословно:

```text
Role: инженер, который проверяет план нагрузочных проверок только по указанным источникам и ничего не додумывает.

Sources (единственное, что можно читать): practice_01/TRAINING_PR.diff, practice_01/problem.md, practice_01/context.md, practice_01/project_management.md.

Question: что из пределов и допущений в practice_01/tests_load.md можно подтвердить источником, а что нет.

Task: возьми каждый предел, каждое число нагрузки и каждое утверждение о коде из tests_load.md. Для подтверждённого дай файл и строку (номер строки самого файла) и короткую цитату. Всё, чего в источниках нет, напиши как «не подтверждено» и отклони; знания из общих источников (например, как FastAPI запускает sync-функции) в подтверждение не принимай.

Format: таблица «утверждение из tests_load.md | статус | ссылка и цитата или причина отказа». Потом краткий список того, что отклонено. Без вопросов в конце.
```

## Ответ со ссылками на источники

### Утверждения о коде (`tests_load.md`, столбцы «Основание в коде»)

| Утверждение | Статус | Ссылка и цитата |
|---|---|---|
| `create_review` объявлен как `def` и вызывает `ReviewService.review` | подтверждено | `TRAINING_PR.diff:36-37`: `def create_review(payload: dict) -> dict[str, str]:` и `return review_service.review(payload["diff"])` |
| `review` вызывает `self.llm.generate` | подтверждено | `TRAINING_PR.diff:21`: `answer = self.llm.generate(prompt)` |
| Вызов синхронный | подтверждено косвенно | в diff нет ни `async`, ни `await`; `review` (`:19`) и `generate` (`:10-11`) объявлены через обычный `def` |
| `health` объявлен как `def` и LLM не вызывает | подтверждено | `TRAINING_PR.diff:41-42`: `def health() -> dict[str, str]:` и `return {"status": "ok"}` |
| Весь diff попадает в промпт, длина не проверяется | подтверждено | `TRAINING_PR.diff:20`: `prompt = f"Review this pull request and find problems:\n{diff}"`; `:37` передаёт `payload["diff"]` без проверки |
| Вокруг `self.llm.generate` нет `try/except` | подтверждено | `TRAINING_PR.diff:19-22`: метод из трёх строк, обработки нет; `grep` по `try`, `except`, `timeout`, `retry` в diff совпадений не дал |
| Что клиент получит при исключении LLM, в diff не определено | подтверждено как отсутствие | в `TRAINING_PR.diff` такого кода нет; `project_management.md:20` планирует «Контролируемая ошибка вместо `KeyError` и при сбое LLM» с пометкой «запланировано, допущение», то есть сейчас она не реализована |
| Реализации LLM и `app.dependencies` в diff нет | подтверждено | `context.md:13`: «реализация `LLM` и `app.dependencies` в diff отсутствует»; `context.md:59` то же |

### Пределы

| Утверждение | Статус | Ссылка или причина отказа |
|---|---|---|
| L1: предел «не задан» | подтверждено | ни в diff, ни в `problem.md`, ни в `context.md` нет ни задержек, ни доли ошибок; `grep` по `p95`, `SLA`, `таймаут`, `timeout` в четырёх файлах пустой |
| L2: `GET /health` отвечает 200 не дольше 1 с | не подтверждено | числа 1 с нет ни в одном источнике. Источник есть только у части «`health` не зависит от LLM» (`TRAINING_PR.diff:41-42`) |
| L2: «ожидаю время в разы короче 5 с задержки LLM» | не подтверждено | это вывод из того, что sync `def` в FastAPI идут в пул потоков. В моих источниках про пул потоков нет ни слова, а размер пула неизвестен (`grep` по `thread`, `pool`, `поток` пустой) |
| L2/L4: `/health` останется доступным при медленной или падающей LLM | не подтверждено | из кода следует только, что `health` не обращается к LLM. Что он не будет ждать, если потоки заняты, из источников не следует |
| L3: предела размера diff нет | подтверждено | `TRAINING_PR.diff:37` без проверки длины; `problem.md:15`: «Реальная команда, лимит и размер diff неизвестны»; `context.md:56`: «Лимит и типичный размер diff неизвестны» |
| Ответ 500 при исключении LLM | не подтверждено | в источниках такого нет. `tests_load.md:12` уже называет это поведением FastAPI по умолчанию и «проверю запуском»; я это оставил как есть |

### Числа нагрузки

| Число | Статус | Причина |
|---|---|---|
| L1: 10 клиентов, 2 минуты, diff около 5 КБ | не подтверждено | нет источника. В `tests_load.md` помечено «Допущение», причина приведена. Оставляю допущением |
| L2: 20 клиентов, 5 минут, задержка 5 с | не подтверждено | то же. Дополнительно: 20 клиентов подобраны под неизвестный размер пула (это в самом файле сказано) |
| L3: 100 КБ и 1 МБ, по 10 запросов | не подтверждено | `context.md:56`: реальный размер неизвестен, учебный diff маленький; числа мои |
| L4: 20% сбоев | не подтверждено | частота сбоев провайдера в источниках отсутствует, допущение |
| «k6 или locust» | не подтверждено | инструмент в источниках не назван, это допущение, так и записано |

### Остальные утверждения в файле

| Утверждение | Статус | Ссылка или причина |
|---|---|---|
| «В `TRAINING_PR.diff` нет данных о реальной нагрузке, SLA, размере diff и параметрах LLM» | подтверждено | весь diff это 42 строки (`TRAINING_PR.diff:1-42`), таких данных в нём нет; про размер и лимит `problem.md:15`, `context.md:56` |
| Инкремент 4 упоминает нагрузочные проверки | подтверждено | `project_management.md:21`: «нагрузочные проверки `tests_load.md` при необходимости» |
| Условие «появится реальная команда с потоком PR и известными лимитами провайдера» | не подтверждено | `project_management.md:21` говорит только «при необходимости» и ничего не уточняет. Условие моё. Обратное подтверждено: `context.md:55` «реальной команды нет, кейс учебный» |
| «До этого достаточно L1 и L3» | не подтверждено | в источниках нет, это мой выбор |
| Реальные вызовы LLM платные, лимиты провайдера неизвестны | не подтверждено | про стоимость в источниках ничего нет. Лимиты провайдера в источниках тоже не названы; известно лишь, что реализации LLM в diff нет (`context.md:13`). В файле помечено «допущение», оставляю |
| Формат ошибки клиенту определит TO BE (L4) | не проверено | `analysis.md` вне моего набора источников |
| Ссылка «строка 12 (P1-04)» в разделе «Как использовали AI» | подтверждено | `prompts.md:12` начинается с `\| P1-04 \|` |

### Проверка ссылок

Для каждой ссылки выше искал фрагмент через `grep -n -F`. Все 20 проверенных фрагментов нашлись на указанных строках:

- `TRAINING_PR.diff`: `@app.post("/api/reviews")` на 35; `def create_review(payload: dict)` на 36; `return review_service.review(payload["diff"])` на 37; `def review(self, diff: str)` на 19; строка с `prompt = f"Review this pull request..."` на 20; `answer = self.llm.generate(prompt)` на 21; `@app.get("/health")` на 40; `def health()` на 41; `return {"status": "ok"}` на 42; `def generate(self, prompt: str) -> str:` на 10 и 11.
- `problem.md`: «Реальная команда, лимит и размер diff неизвестны» на 15; «аутентификация и лимиты самого сервиса» на 11.
- `context.md`: «Лимит и типичный размер diff неизвестны» на 56; «реализация `LLM` и `app.dependencies` в diff отсутствует» на 13; «реализация `LLM` и `app.dependencies.review_service` вне diff неизвестна» на 59; «Допущение: реальной команды нет, кейс учебный.» на 55.
- `project_management.md`: «нагрузочные проверки `tests_load.md` при необходимости» на 21; «Контролируемая ошибка вместо `KeyError` и при сбое LLM» на 20.
- `prompts.md`: `| P1-04 |` на 12.

Расхождений не нашёл. Отдельно проверил, что «нет» тоже верно: `grep -i` по `async|await|try|except|len\(|timeout|thread|pool|sla|p95|retry` в diff и по `sla|p95|таймаут|timeout|поток|thread|нагрузк|токен|провайдер` в остальных трёх файлах дал пустой результат (слово «нагрузк» встречается только в `project_management.md:21` как ссылка на `tests_load.md`, то есть без чисел).

## Что изменили в исходном артефакте

Правка применена на финальном шаге, в самом эксперименте файл не менялся. Номера строк `tests_load.md` в моих замечаниях выше и в ответе относятся к файлу на момент чтения, до правок; после них строки сместились. Номера строк источников (`TRAINING_PR.diff`, `problem.md`, `context.md`, `project_management.md`, `prompts.md`) я после правок перепроверил `grep -n -F`, они не изменились.

- Файл и раздел: [`practice_01/tests_load.md`](../../practice_01/tests_load.md): новый раздел «Что подтверждено источниками» (после таблицы), ячейка L2 в столбце «Источник предела», раздел «Когда нагрузочное тестирование нужно» и «Как использовали AI».
- Изменение (применено):
  1. Добавлен раздел «Что подтверждено источниками»: три списка (подтверждено с номерами строк; не подтверждено и оставлено допущением; не подтверждено и не считаю фактом), как в моём ответе выше.
  2. В L2 в «Источник предела» дописано: ожидание «в разы короче задержки LLM» держится на пуле потоков, в источниках про пул ничего нет, поэтому это гипотеза, которую проверит запуск.
  3. Абзац про условие нагрузочного тестирования переписан: `project_management.md:21` говорит только «при необходимости», условие про реальную команду с потоком PR и лимитами провайдера помечено моим допущением, а не источником; ссылка на `context.md:55` («реальной команды нет»). Эту же строку правили CoVe (про инкремент 4) и ReAct; я свёл три правки в один абзац без дублей.
  4. В «Как использовали AI» добавлен RAG со ссылкой на этот файл и описание, что подтвердилось и что осталось допущением.
- Как проверили ссылки: открыл каждый источник и нашёл все 20 цитируемых фрагментов через `grep -n -F` на указанных строках (список выше). Проверил и отсутствие в источниках слов про пул потоков, SLA, p95, таймаут, `try/except`. Это проверка чтением и `grep`; нагрузку я не запускал.
- Что отклонил как неподтверждённое: предел 1 с для `/health`; «время `/health` в разы короче задержки LLM» и вообще пул потоков для sync `def` в FastAPI; ответ 500 при исключении LLM; все числа нагрузки (10, 20, 5 КБ, 100 КБ, 1 МБ, 2 и 5 минут, 20%); условие «реальная команда с потоком PR и известными лимитами» как требование из источников; «достаточно L1 и L3»; платность вызовов LLM; инструмент k6 или locust. Формат ошибки клиенту по TO BE не проверял, `analysis.md` вне набора источников.
