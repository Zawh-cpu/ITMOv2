# ReAct

Кто выполнял. Этот эксперимент выполнил Claude Sonnet 5 Thinking внутри сессии финальной сборки Практики 2, без отдельного запуска и без другой внешней модели. `cmd.txt` и `result.jsonl` поэтому нет. Шаги ниже это реальные чтения файлов и `grep` в `practices/practice_01`; сервис и нагрузка не запускались.

- Цель: найти, где `tests_load.md` расходится с остальными артефактами Практики 1 (метрики из `problem.md`, `tests_integration.md`, `tests_e2e.md`, `adr.md`, `context.md`) и с `TRAINING_PR.diff`. Расхождение нужно найти по каждой из пяти метрик `problem.md`.
- Доступные входы: файлы в `practices/practice_01` (`tests_load.md`, `problem.md`, `tests_integration.md`, `tests_e2e.md`, `adr.md`, `context.md`, `TRAINING_PR.diff`, при необходимости `project_management.md`, `product_management.md`, `prompts.md`).
- Разрешённые действия: чтение файлов и `grep` в `practices/practice_01`.
- Запрещённые действия: правка любых файлов Практики 1, обращение к сети, запуск сервиса и нагрузки.
- Максимальное число шагов: 8.
- Условие остановки и вопроса человеку: остановиться, когда по каждой метрике найдено расхождение, или когда упёрся в лимит шагов; если для вывода нужен внешний факт (SLA, реальные лимиты, поведение FastAPI при запуске), не додумывать, а задать вопрос человеку.

## Запрос

Практика 2, эксперимент ReAct. Выбранный артефакт: `practices/practice_01/tests_load.md`. Цель: найти, где `tests_load.md` расходится с остальными артефактами Практики 1 (`problem.md` метрики, `tests_integration.md`, `tests_e2e.md`, `adr.md`, `context.md`) и `TRAINING_PR.diff`. Разрешённые действия: только чтение файлов и grep в `practices/practice_01`; запрещены правка файлов, сеть, запуск сервиса. Максимум 8 шагов. Остановка: нашёл расхождение по каждой метрике, или упёрся в лимит, или нужен внешний факт (тогда вопрос человеку). На каждом шаге фиксируй действие (реальный вызов), наблюдение (что вернулось) и решение продолжать или остановиться.

## Наблюдаемые действия и результаты

Метрики `problem.md` для краткости: М1 доля рисков с файлом, строкой и цитатой; М2 доля подтверждённых ревьюером; М3 доля ответов в формате; М4 время ревьюера на PR; М5 доля выдуманных фактов.

| Шаг | Действие | Наблюдение | Решение продолжить или остановиться |
|---|---|---|---|
| 1 | Read `practice_01/tests_load.md` | Четыре сценария L1-L4. Измеряется только задержка, доля ошибок, CPU, доступность `/health`, размер промпта. Метрик из `problem.md` в таблице не называю (это позже проверил grep на шаге 7). Строка 12: код 500 при исключении LLM «не заявляю как факт», это поведение FastAPI по умолчанию. Строка 23: нагрузка нужна, когда появится реальная команда с потоком PR (инкремент 4). | Продолжить: нужны метрики и остальные артефакты. |
| 2 | Bash `cat problem.md` (чтение) | Пять метрик М1-М5, все про качество ответа модели и работу ревьюера. Метрик задержки, ошибок, пропускной способности нет. Вне задачи: «аутентификация и лимиты самого сервиса». Текущие значения неизвестны, цели 100% и 70% помечены допущением. | Продолжить. Первая гипотеза: пределы L1-L4 не привязаны ни к одной из М1-М5. |
| 3 | Read `TRAINING_PR.diff` | 42 строки. `create_review(payload: dict)` объявлена как `def`, вызывает `review_service.review(payload["diff"])`; `review()` строит промпт `f"Review this pull request and find problems:\n{diff}"` и синхронно вызывает `self.llm.generate`. `health()` тоже `def`, LLM не трогает. `try/except` нет, проверки длины diff нет, пула потоков и таймаутов в diff нет. | Продолжить. Основания L1-L4 в столбце «Основание в коде» с diff совпадают. Сам код подтверждает и то, что сервис использует простой промпт, а не Master Prompt v1.1: значит, метрики М1, М3, М5 на текущем коде нагрузкой не проверить. |
| 4 | Bash `cat -n tests_integration.md tests_e2e.md` | I4 и e2e «сбой LLM»: «Текущее поведение по diff: 500». То есть там 500 подан как вывод из diff, а в `tests_load.md` (строка 12) то же самое явно не факт. Это расхождение формулировок. E2E-граничный сценарий: пустой diff и 1 МБ, ссылка на L3. В L3 пустого diff нет, размеры 100 КБ и 1 МБ. I6 проверяет `/health` на 200 и `{"status": "ok"}`, L2 и L4 предел для `/health` задают по-другому: L2 «200 не дольше 1 с», L4 просто «200». I7 и e2e проверяют М1, М3, М5 по чек-листу формата, в L1-L4 этого нет. | Продолжить. Записал: (а) 500 подан по-разному в трёх файлах, (б) L3 не покрывает пустой diff из e2e, (в) I7 и e2e выходят на метрики качества, load нет. |
| 5 | Bash `cat -n adr.md context.md` | ADR, п. 5: контракт ответа `{"comment": ...}` не меняется, в ограничениях: формат держится только на промпте, один маленький diff, одна модель. В ADR и `context.md` про нагрузку, задержку, пул потоков, размер diff нет ни слова. `context.md`, раздел «Что пока неизвестно»: «Допущение: реальной команды нет, кейс учебный» и «Лимит и типичный размер diff неизвестны. Допущение: как в учебном кейсе, diff маленький; для больших diff поведение не проверялось». | Продолжить. Расхождения: (а) `context.md` считает реальную команду отсутствующей, `tests_load.md` (строка 23) ставит условием её появление, а вопросы про поток PR и лимиты провайдера адресует владельцу сервиса, которого в артефактах нет (в ADR указан только автор решения); (б) L3 берёт 100 КБ и 1 МБ, тогда как context фиксирует «diff маленький» и для больших не проверялось; L1 (около 5 КБ) с этим согласуется. |
| 6 | Bash `grep -n -i -E "500\|p95\|таймаут\|latency\|задержк\|нагрузк\|1 МБ\|КБ\|health"` по `adr.md context.md problem.md tests_integration.md tests_e2e.md README.md project_management.md product_management.md analysis.md` | 11 строк: только `context.md:12` (про «задержку» в смысле долгого ревью), `tests_integration.md` (I2, I4, I6, I7 и вводный абзац), `tests_e2e.md` (три строки). В `problem.md`, `adr.md`, `README.md`, `analysis.md` совпадений нет. Слово «задержка» в `context.md` означает время ревьюера, а не задержку ответа сервиса. | Продолжить. Подтвердилось: ни одна метрика `problem.md` не выражена в терминах p95, ошибок или таймаутов. Единственное пересечение слова «время» — М4, но L1-L4 меряют время ответа сервиса, а не минуты ревьюера. |
| 7 | Bash: `grep -n -i -E "нкремент\|P1-04\|команд" project_management.md`; `sed -n '12p' prompts.md`; `grep` по словам метрик в `tests_load.md` | В `project_management.md` инкремент 4 есть, но в нём написано «замер метрик на нескольких diff» и «нагрузочные проверки `tests_load.md` при необходимости»; про реальную команду с потоком PR и про лимиты провайдера LLM там нет. Строка 12 `prompts.md` это P1-04, ссылка из `tests_load.md` верна. В `tests_load.md` grep по словам «формат», «цитат», «доказательств», «подтвержд», «выдуман», «время ревьюера», «метрик» вернул только строки 3, 5, 8, 10 (строка 3 «подтверждён владельцем», строка 5 заголовок «Подтверждение», в 8 и 10 слова из других контекстов). Ни одна из М1-М5 в файле не названа. | Продолжить. Ссылка на инкремент 4 в строке 23 неточна: условие «реальная команда, лимиты провайдера» придумано в `tests_load.md`, в `project_management.md` его нет. |
| 8 | Bash `sed -n '21p' project_management.md` и `grep -i "нагруз\|tests_load\|лимит"` по `project_management.md analysis.md product_management.md` | Строка 21 инкремента 4 подтверждена дословно. `product_management.md:12` относит «аутентификацию и лимиты самого сервиса» к вне задачи; при этом вопрос владельцу в `tests_load.md` про «лимит размера или токенов» (L3) касается лимита размера diff. В `analysis.md` про нагрузку ничего нет. | Остановился: по всем пяти метрикам расхождение найдено (см. ниже), лимит 8 шагов исчерпан. Вопрос человеку: нужны ли метрики задержки и ошибок сервиса в `problem.md`; без этого пределы L1-L4 нечем подтверждать, а решать это должен владелец, не я. |

### Итог по метрикам `problem.md`

| Метрика | Расхождение с `tests_load.md` |
|---|---|
| М1 доля рисков с файлом, строкой, цитатой | L1-L4 её не измеряют; на простом промпте из diff она не определена. Проверяется только в `tests_e2e.md` (позитивный сценарий) и I7. |
| М2 доля подтверждённых ревьюером | Не измеряется нагрузкой: подтверждает ревьюер вручную (`problem.md`), в столбце «Подтверждение» L1-L4 стоит «отчёт запуска». |
| М3 доля ответов в формате | Не измеряется. L1-L4 идут на fake LLM, у которой формата нет, значит, формат нагрузкой не проверить в принципе. |
| М4 время ревьюера | В L1-L4 «время ответа» это задержка сервиса. Названия похожи, а измеряется другое; связи между p95 сервиса и минутами ревьюера в файле нет. |
| М5 доля выдуманных фактов | Не измеряется; L3 (большой diff) косвенно относится сюда, если модель на 1 МБ начнёт выдумывать, но в столбце «Что измеряем» этого нет. |

Итог: у нагрузочных проверок нет ни одной метрики из `problem.md`. Все L1-L4 держатся на допущениях и вопросах владельцу, а в `problem.md` метрик производительности нет вообще.

Вторичные расхождения с другими артефактами:

- Код 500: в `tests_integration.md` (I2, I4) и `tests_e2e.md` написано «текущее поведение по diff: 500», в `tests_load.md` (строка 12) это же названо не подтверждённым фактом. В diff действительно нет ни 500, ни `try/except`; правильнее формулировка из load.
- L3 не содержит пустой diff, а `tests_e2e.md` его включает в тот же граничный сценарий со ссылкой на L3.
- Предел `/health`: L2 «200 не дольше 1 с», L4 «200» без времени, I6 «200 и `{"status": "ok"}`».
- Условие из строки 23 («реальная команда с потоком PR и известными лимитами») в `project_management.md` не записано, а `context.md` фиксирует, что реальной команды нет.
- Вопросы адресованы «владельцу сервиса», а в ADR указан только автор решения; отдельного владельца сервиса в артефактах нет.

## Что изменили в исходном артефакте

Правки применены на финальном шаге, в самом эксперименте файлы не менялись. Номера строк `tests_load.md` в шагах 1, 5, 7 и в предложенном тексте («строка 12», «строка 23» и т. п.) относятся к файлу на момент чтения, до правок; после них строки сместились.

- Файл и раздел: [`practice_01/tests_load.md`](../../practice_01/tests_load.md): новый раздел «Связь с метриками `problem.md`» после таблицы, абзац про код 500, ячейка L3, раздел «Когда нагрузочное тестирование нужно», вопросы владельцу. Связанные файлы: [`tests_integration.md`](../../practice_01/tests_integration.md) (I2, I4) и [`tests_e2e.md`](../../practice_01/tests_e2e.md) (два негативных сценария).
- Изменение (применено):
  1. Добавлен раздел «Связь с метриками `problem.md`»: L1-L4 не измеряют ни одну из пяти метрик, «время ответа» это время сервиса, метрик производительности в `problem.md` нет, качество проверяют I7 и e2e.
  2. В L3 добавлен пустой diff `""` из граничного сценария `tests_e2e.md`.
  3. Абзац про код 500 переписан: не факт из diff, а моё ожидание по поведению FastAPI по умолчанию (общее знание), проверить запуском. Предложение из эксперимента было только записать расхождение. При итоговой сверке я решил согласовать формулировки: в `tests_integration.md` (I2, I4) и `tests_e2e.md` слова «Текущее поведение по diff: 500» заменены на «Ожидание: в diff нет обработки, ответ 500 (поведение FastAPI по умолчанию, проверить запуском)». Это уже мой выбор на финальном шаге, не результат самого эксперимента.
  4. Условие про реальную команду сведено в один абзац вместе с правками RAG и CoVe: в артефактах Практики 1 оно не записано, `project_management.md:21` говорит «при необходимости», предложение помечено допущением.
  5. Пятым пунктом в вопросы владельцу добавлено про метрики задержки и доли ошибок сервиса в `problem.md`.
- Как проверили: каждое расхождение получено чтением и `grep` (шаги 1-8). После применения правок я повторил `grep -n "допущение\|problem.md"` по `tests_load.md` и `make step1`. Запуск нагрузки не выполнялся.
- Что отклонили:
  - Добавить в L1-L4 метрики М1-М5 как измеряемые: мок LLM не имеет формата и цитат, нагрузка их не проверит.
  - Дописать в `problem.md` метрики p95 и доли ошибок самому: это решение владельца, вопрос вынесен человеку (шаг 8).
  - Считать 500 фактом: в diff его нет.
  - Другие правки в `tests_integration.md` и `tests_e2e.md`: только формулировка про 500, остальное не трогал.
