# Нагрузочные проверки

В `TRAINING_PR.diff` нет данных о реальной нагрузке, SLA, размере diff и параметрах LLM. Все числа ниже — допущения для расчёта плана, а не требования проекта; они подлежат уточнению. Запуск не выполнялся.

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Подтверждение |
|---|---|---|---|---|
| L1. Накладные расходы сервиса (fake LLM без задержки) | Допущение: 10 параллельных клиентов, 2 минуты, `POST /api/reviews` с diff около 5 КБ | Допущение: p95 не более 500 мс, доля ошибок 0% | p50 и p95 задержки, доля ошибок, CPU | Нагрузочный инструмент (допущение: k6 или locust), отчёт запуска |
| L2. Медленная LLM (fake LLM с задержкой 5 с) | Допущение: 20 параллельных клиентов, 5 минут | Допущение: доля таймаутов 0%, `GET /health` отвечает не дольше 1 с во время нагрузки | Задержка `/health`, число одновременных запросов, ошибки | Инструмент и `GET /health` параллельно. Обе функции в diff объявлены как `def`, поведение пула потоков не проверялось |
| L3. Большой diff | Допущение: diff 100 КБ и 1 МБ, по 10 запросов последовательно | Предел размера diff неизвестен (вопрос владельцу); в diff ограничения размера нет. Пока фиксируем наблюдаемое поведение | Время ответа, размер промпта, ошибки | Замер запуска |
| L4. Сбой LLM под нагрузкой | Допущение: fake LLM бросает исключение в 20% запросов, 10 клиентов, 2 минуты | Допущение: сервис остаётся доступным (`/health` 200); формат ошибки клиенту будет определён в TO BE | Доля 5xx, доступность `/health` | Нагрузочный инструмент |

Стоимость и лимиты реальной LLM в нагрузочных проверках не используются: лимиты провайдера неизвестны, реальные вызовы платные (допущение).

Условие, когда нагрузочное тестирование необходимо: появится реальная команда с потоком PR и известными лимитами провайдера LLM (инкремент 4 в [`project_management.md`](project_management.md)). До этого достаточно L1 и L3 на учебных данных.

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): строка 12 (P1-04).
- Что я проверил и какие исправления поручил: на момент записи я пределы не оценил; все числа помечены допущениями.
